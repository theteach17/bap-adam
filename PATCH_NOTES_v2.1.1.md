# Patch v2.1.1 — แก้ปัญหาปุ่มเมนูไม่ทำงาน และลดเวลาโหลดหน้า

แพตช์นี้แก้เฉพาะ **ประสิทธิภาพ** และ **กลไกการเปลี่ยนหน้า** เท่านั้น
ไม่แตะ workflow, สิทธิ์, โครงสร้างชีต, หัวคอลัมน์ หรือข้อความบนหน้าจอเดิม

---

## 1. ไฟล์ที่เปลี่ยน

| ไฟล์ | ประเภทการแก้ |
|---|---|
| `PerfCache.gs` | **ไฟล์ใหม่** — แคชภายในการทำงาน 1 ครั้ง |
| `ConfigService.gs` | จำค่าคอนฟิก 1 ชุดต่อ execution |
| `PrecheckRepository.gs` | จำ handle สเปรดชีต / ชีต / ผลอ่านแถว + ล้างแคชทุกครั้งที่เขียน |
| `AccessControlService.gs` | จำ principal ต่อ execution (ผูกกับ userKeyHash) |
| `Utils.gs` | ล้างแคชแถวเมื่อเข้า critical section |
| `รหัส.gs` | จำ handle สเปรดชีตหลัก / session / settings, แคชตัวเลขหน้าแรก, ส่ง baseUrl ให้หน้าเว็บ, รวมการเขียน session เป็นครั้งเดียว |
| `SharedScripts.html` | `nav()` เปลี่ยนหน้าแบบไม่ต้องรอเซิร์ฟเวอร์ + watchdog |
| `Index.html` | แก้ `goPage()` ที่ทำให้ปุ่มเมนูค้าง + แก้ logout |
| `AccessDenied.html` | ปุ่มกลับหน้าหลักไม่ต้องรอเซิร์ฟเวอร์ |
| ทุกหน้า `.html` | ฝัง `window.__PC_BASE_URL` ตั้งแต่ตอน render |

---

## 2. วิธีติดตั้ง

1. เปิด Apps Script project เดิม
2. **สร้างไฟล์ใหม่ชื่อ `PerfCache`** (ประเภท Script) แล้ววางเนื้อหาจาก `PerfCache.gs`
3. แทนที่เนื้อหาไฟล์ที่เหลือตามตารางข้อ 1 (คัดลอกทับทั้งไฟล์)
4. **Deploy → Manage deployments → แก้ไข deployment เดิม → New version**
   (ต้องสร้าง version ใหม่ ไม่เช่นนั้นผู้ใช้จะยังได้โค้ดเดิม)
5. ไม่ต้องแก้ Script Properties, ไม่ต้องแก้ชีต, ไม่ต้องรัน migration ใด ๆ

> **สำคัญ:** URL ของเว็บแอปต้องเป็นตัวเดิม แพตช์นี้ใช้ `ScriptApp.getService().getUrl()`
> ซึ่งเป็นค่าเดียวกับที่ `getAppNavigationUrl()` เดิมใช้อยู่แล้ว

---

## 3. วิธีถอยกลับ (Rollback)

Deploy → Manage deployments → เลือก version ก่อนหน้า
ไม่มีการเปลี่ยนแปลงข้อมูลใด ๆ ที่ต้องย้อนคืน

---

## 4. สิ่งที่ต้องทดสอบหลังติดตั้ง (Smoke test)

- [ ] เข้าสู่ระบบ → เข้าหน้าหลักได้ตามปกติ
- [ ] กดเมนู "ส่งเอกสาร" / "รายการเอกสารของฉัน" ติดกันหลายครั้ง → ต้องไปได้ทุกครั้ง ไม่ต้องรีเฟรช
- [ ] เจ้าหน้าที่: เปิดหน้า "ตรวจรายงาน Pre-check" → เปิดเอกสาร → บันทึกร่าง → สรุปผล
- [ ] ผู้ใช้ทั่วไป: ต้องไม่เห็นเมนูเจ้าหน้าที่ และถ้าเปิด URL ตรงต้องได้หน้า "สิทธิ์ไม่เพียงพอ"
- [ ] ออกจากระบบ → กลับไปหน้า Login และเข้าหน้าเดิมซ้ำไม่ได้
- [ ] ขอเลขทะเบียนเอกสารใหม่ → ตัวเลขบนแถบวิ่งหน้าแรกอัปเดต

---

## 5. หมายเหตุพฤติกรรมที่เปลี่ยนเล็กน้อย (โดยตั้งใจ)

| จุด | เดิม | ใหม่ | เหตุผล |
|---|---|---|---|
| คอลัมน์ `LastAction` ในชีต Sessions | บันทึกชื่อฟังก์ชัน**สุดท้าย**ที่ตรวจสิทธิ์ (มักเป็น `principal`) | บันทึกชื่อฟังก์ชัน**แรก**ของคำขอ (เช่น `getOfficerDashboard`) | สื่อความหมายตรงกว่า และลดการอ่านชีต Sessions ซ้ำ 2–4 รอบต่อคำขอ |
| ตัวเลขสรุปหน้าแรก | คำนวณใหม่ทุกครั้ง | แคช 60 วินาที และล้างทันทีเมื่อออกเลขทะเบียนใหม่ | ลดการอ่านชีต ReportNo ทั้งใบทุกครั้งที่เปิดหน้าหลัก |
| กดเมนูแล้วสิทธิ์ไม่พอ | ขึ้น toast แจ้งเตือน | เข้าหน้า "สิทธิ์ไม่เพียงพอ" | เมนูเหล่านั้นซ่อนอยู่แล้วสำหรับผู้ไม่มีสิทธิ์ และ `doGet()` ยังกันไว้ครบ |

---

## 6. สิ่งที่ **ไม่ได้** แก้ในรอบนี้ (เสนอไว้พิจารณารอบถัดไป)

1. ชีต `Sessions` โตขึ้นเรื่อย ๆ (ปัจจุบัน 225 แถว) และถูกอ่านทั้งใบทุกคำขอ
   → ควรมี trigger ย้ายแถวที่หมดอายุไปชีตเก็บถาวรเดือนละครั้ง
2. `pcAudit_('REVIEW_AUTOSAVED')` เขียน 1 แถวทุกครั้งที่ autosave (ทุก 2.5 วินาที)
   → ควร throttle เหลือ 1 ครั้งต่อ 5 นาทีต่อ review
3. หน้าแรกยิง `google.script.run` 3 ครั้งพร้อมกันตอนโหลด
   → รวมเป็น `getIndexBootstrap()` ครั้งเดียวได้
4. `setResult()` ในหน้าตรวจ วาด checklist ใหม่ทั้งชุดทุกครั้งที่กดผล
   → ควรอัปเดตเฉพาะการ์ดที่กด
