# PATCH NOTES — STATBAR (Release A)

**ระบบ:** Project Adam — ศูนย์สารสนเทศกลาง
**Baseline:** package `bap-adam-upgrade-faster` (v2.1.x)
**ไฟล์ที่ต้องแทนที่:** `Index.html` เพียงไฟล์เดียว
**SHA256:** `be1648eade22e67652249902bc0b83f26afac50b5b9ed5d5ff40af75374018a9`
**ขนาด:** 2,398 บรรทัด (เดิม 2,259)

---

## 1. สาระของการเปลี่ยนแปลง

ถอดแถบข้อความวิ่ง (Footer Marquee) ที่อยู่ล่างจอออก แล้วย้ายข้อมูลไปไว้ในรูปแบบที่เหมาะสมกว่า

| เนื้อหาเดิมใน Marquee | ย้ายไปที่ |
|---|---|
| โลโก้โรงเรียน | ตัดออก (ซ้ำกับ sidebar header ที่มีอยู่แล้ว) |
| ข้อความต้อนรับ + ชื่อหน่วยงาน | ตัดออก (ซ้ำกับ sidebar header และ logout-section) |
| คำขวัญ "รวดเร็ว ฉับไว โปร่งใส ตรวจสอบได้" | ย้ายไป `.logout-section` ท้าย sidebar เป็นข้อความนิ่ง |
| ตัวเลขสถิติ 5 กลุ่ม | แถบชิป `#statBar` เหนือตารางเอกสาร **และกดกรองตารางได้** |

---

## 2. เหตุผล

1. **ตัวเลขอ่านได้ทันที** — เดิมต้องรอข้อความวิ่งครบรอบ 10–20 วินาทีจึงจะเห็นเลขที่ต้องการ
2. **คืนมุมล่างขวา** — เตรียมพื้นที่ให้วิดเจ็ตผู้ช่วยใน Release B และคืน `padding-bottom` 50px ที่เคยกันไว้ให้แถบ fixed
3. **`marquee` เป็น deprecated element** — เบราว์เซอร์อาจเลิกรองรับในอนาคต
4. **การเข้าถึง** — ข้อความเคลื่อนไหวอัตโนมัติขัด WCAG 2.2.2 (Pause, Stop, Hide)
5. **ตัวเลขกลายเป็นเครื่องมือ** — จากข้อมูลประดับเป็นปุ่มกรองตาราง

---

## 3. รายการแก้ไขทั้งหมด (10 จุด)

| # | ตำแหน่งเดิม | การแก้ไข |
|---|---|---|
| 1 | CSS `.main-content` บรรทัด 166 | `padding: 0 0 50px 0` → `0 0 16px 0` (คืนพื้นที่ที่กันไว้ให้ marquee) |
| 2 | CSS บรรทัด 388–406 | ลบ `.footer-marquee` และ `.footer-marquee img` แทนด้วยชุด `.statbar-*` |
| 3 | CSS `@media (max-width: 992px)` บรรทัด 459 | ลบกฎ `.footer-marquee` แทนด้วยกฎย่อขนาดชิปบนมือถือ |
| 4 | HTML `.logout-section` บรรทัด 527 | เพิ่มคำขวัญ 1 บรรทัด |
| 5 | HTML `.content-wrapper` บรรทัด 558 | แทรก `<div class="statbar" id="statBar">` ก่อน `.content-card` |
| 6 | HTML บรรทัด 595–601 | ลบบล็อก `.footer-marquee` ทั้งหมด คงไว้เฉพาะ `</div> </div>` ที่ปิด `.content-wrapper` และ `.main-content` |
| 7 | JS `window.onload` บรรทัด 919 | `updateMarquee()` → `updateStatChips()` |
| 8 | JS บรรทัด 1089 | เพิ่ม event listener ตัวที่สองบนช่องค้นหา (ไม่แตะ listener เดิม) |
| 9 | JS บรรทัด 1795–1799 | แทน `updateMarquee()` ด้วย 5 ฟังก์ชันใหม่ |
| 10 | JS บรรทัด 1919, 2219, 2233 | `updateMarquee()` → `updateStatChips()` (3 จุด หลังบันทึกข้อมูล) |

### ฟังก์ชันใหม่

| ฟังก์ชัน | หน้าที่ |
|---|---|
| `STATBAR_GROUPS` | ตารางกำหนดชิป 5 ใบ (label / field / คำค้นสำหรับกรอง) |
| `updateStatChips()` | ดึงข้อมูลจาก `getReportCounts()` — **แทนที่ `updateMarquee()` โดยตรง** |
| `renderStatChips(counts)` | สร้างปุ่มชิปด้วย DOM API และ `textContent` |
| `applyStatChipFilter(text)` | กรองตารางผ่านช่องค้นหาเดิม + `fetchData()` เดิม |
| `syncStatChipState()` | ซิงก์ไฮไลต์และ `aria-pressed` กับคำค้นปัจจุบัน |
| `setStatBarState(msg)` | แสดงสถานะกำลังโหลด / ข้อผิดพลาด |

---

## 4. สิ่งที่ **ไม่ได้** แตะเลย

- ไฟล์ `.gs` ทุกไฟล์ — **ไม่มีการแก้ฝั่งเซิร์ฟเวอร์แม้แต่บรรทัดเดียว**
- `appsscript.json`, OAuth scopes, deployment config
- `getReportCounts()`, `getData()`, `saveData()`, `saveReport()`, `saveNonCompletedProject()` — ใช้สัญญาเดิมทุกฟิลด์
- ไฟล์ HTML อื่นทั้งหมด (`SubmitDocument`, `MyDocuments`, `Precheck*`, `Login`, `Shared*`)
- `PROTECTED_PAGES`, `PC_NAV_PAGES`, `__PC_NAV_PAGES` — ไม่มีหน้าใหม่
- โครงสร้าง sidebar, top-bar, modal, ตาราง, pagination, การเรียงลำดับ, การอัปโหลดไฟล์
- schema ชีต, trigger, ข้อมูลใด ๆ

**ไม่ต้องรัน migration ไม่ต้องตั้งค่าใหม่ ไม่ต้องขอสิทธิ์เพิ่ม**

---

## 5. ผลการตรวจสอบ (7 รอบ)

| ชุดตรวจ | ผล |
|---|---|
| โครงสร้าง HTML (สมดุล tag) | ผ่าน — เทียบเท่าต้นฉบับทุกประการ |
| CSS braces | ผ่าน — 79/79 สมดุล |
| CSS parser (tinycss2) | ผ่าน — 68 rules, 0 errors |
| ไวยากรณ์ JavaScript (node --check) | ผ่าน — ทั้ง 2 script blocks |
| การอ้างอิงฟังก์ชัน/ID | ผ่าน — ชุดที่ไม่พบตรงกับต้นฉบับเป๊ะ |
| ความครบถ้วนเทียบต้นฉบับ | ผ่าน — id/handler/input/name ไม่หายโดยไม่ตั้งใจ, nav-btn 8→8, modal 3→3, table 1→1, button 24→24 |
| encoding ภาษาไทย | ผ่าน — ไม่มี U+FFFD, NBSP, zero-width, ไม่มีวรรณยุกต์ลอย |
| ขอบเขต CSS | ผ่าน — selector ใหม่ทุกตัวขึ้นต้นด้วย `.statbar` ไม่มี selector ระดับ element |
| พฤติกรรมใน DOM จำลอง | ผ่าน 48/48 เคส |
| เชิงโจมตี/กรณีขอบ | ผ่าน 11/11 เคส |

**รวม 59 เคส ผ่านทั้งหมด**

### ข้อบกพร่องที่พบระหว่างตรวจและแก้แล้ว

| รอบ | สิ่งที่พบ | การแก้ |
|---|---|---|
| 1 | เขียน tag `marquee` ไว้ในคอมเมนต์ภายใน `<style>` | เปลี่ยนเป็นข้อความธรรมดา |
| 1 | ชิปไม่มี `aria-pressed` | เพิ่มและซิงก์กับสถานะไฮไลต์ |
| 3 | ตารางขยับตอนสถิติโหลดเสร็จ (layout shift) | เพิ่ม `min-height` จองความสูงไว้ล่วงหน้า |
| 5 | ค่าที่แปลงเป็นตัวเลขไม่ได้แสดงคำว่า "NaN" | บังคับเป็น 0 ด้วย `isFinite()` |
| 5 | ส่ง `null` เข้า `renderStatChips()` โดยตรงแล้ว throw | เพิ่ม `counts = counts \|\| {}` |

---

## 6. ความเข้ากันได้กับ Bootstrap 5.3

`Index.html` โหลด Bootstrap 5.3, jQuery 3.6, SweetAlert2 v11, FontAwesome 6.4 เป็น global

- คลาสใหม่ทุกตัวขึ้นต้นด้วย `statbar-` ซึ่งไม่มีใน Bootstrap
- ใช้ `.is-on` แทน `.active` เพื่อเลี่ยงคลาสที่ Bootstrap ใช้อยู่
- ปุ่มชิปกำหนด `background`, `border`, `color`, `font-family: inherit`, `font-size`, `line-height`, `padding`, `border-radius` ครบทุกค่าที่ Bootstrap Reboot ตั้งไว้
- ทุกชิปเป็น `type="button"` จึงไม่ submit ฟอร์มใด

---

## 7. ข้อควรทราบ (พฤติกรรมที่ตั้งใจให้เป็นแบบนี้)

1. **ตัวเลขอาจช้าได้ถึง 60 วินาที** — `getReportCounts()` มีแคชฝั่งเซิร์ฟเวอร์ 60 วินาที เป็นพฤติกรรมเดิมของแถบวิ่ง ไม่ได้เปลี่ยน
2. **แถบชิปเลื่อนหายเมื่อสกอลล์** — ต่างจากแถบเดิมที่เป็น `position: fixed` ลอยตลอด เลือกแบบนี้เพราะทำ sticky ใต้ `.top-bar` ที่ความสูงไม่คงที่บนมือถือมีความเสี่ยงสูงกว่าประโยชน์
3. **การกรองใช้การค้นหาข้อความทุกคอลัมน์** — หากมีชื่อเอกสารที่บังเอิญมีคำว่า "กลุ่มบริหารวิชาการ" อยู่ ก็จะติดมาด้วย ในทางปฏิบัติแทบไม่เกิด และเป็นการแลกกับการที่ไม่ต้องแก้ฝั่งเซิร์ฟเวอร์เลย
4. **การพิมพ์ค้นหาเองไม่รีเซ็ตหน้า** — เป็นพฤติกรรมเดิมของระบบที่ **จงใจไม่แก้** เพราะอยู่นอกขอบเขตงานนี้ (การกดชิปรีเซ็ตเป็นหน้า 1 เสมอ)

---

## 8. ขั้นตอนติดตั้ง

1. สำรอง `Index.html` ปัจจุบันเก็บไว้ (ไฟล์เดียวที่เปลี่ยน จึง rollback ง่ายมาก)
2. เปิด Apps Script Editor แล้วแทนที่เนื้อหาไฟล์ `Index.html` ด้วยไฟล์ในชุดนี้
3. Deploy → Manage deployments → แก้ deployment เดิมให้ใช้ Version ใหม่ (รักษา URL เดิม)
4. เปิดหน้าหลักแล้วตรวจตามข้อ 9

**ไม่ต้องรัน** `setupPrecheckSystem()`, `configurePrecheckSystem()`, `migratePrecheckSchema()` หรือฟังก์ชันตั้งค่าใด ๆ

---

## 9. รายการตรวจหลังติดตั้ง (Smoke Test)

**หน้าหลัก**
- [ ] แถบชิปแสดงตัวเลข 5 กลุ่มเหนือตาราง
- [ ] ผลรวม 4 กลุ่มเท่ากับยอด "ทั้งหมด"
- [ ] ไม่มีแถบข้อความวิ่งที่ล่างจอแล้ว
- [ ] คำขวัญปรากฏใต้ปุ่มออกจากระบบใน sidebar

**การกรอง**
- [ ] กดชิปกลุ่มใดกลุ่มหนึ่ง → ตารางกรอง ช่องค้นหาถูกเติมคำ ชิปนั้นไฮไลต์
- [ ] กดชิป "ทั้งหมด" → ล้างตัวกรอง กลับมาแสดงครบ
- [ ] พิมพ์ชื่อกลุ่มในช่องค้นหาเอง → ชิปที่ตรงกันไฮไลต์ตาม

**Regression ของเดิม**
- [ ] ขอเลขทะเบียนเอกสาร → บันทึกสำเร็จ → ตัวเลขในชิปอัปเดต (อาจช้าถึง 60 วินาทีตามแคชเดิม)
- [ ] ส่งเอกสาร / รายการเอกสารของฉัน / ตรวจรายงาน / จัดการแบบตรวจ เปิดได้ตามสิทธิ์เดิม
- [ ] แจ้งกิจกรรมที่ไม่ได้ดำเนินการ → บันทึกได้
- [ ] ค้นหา / เรียงคอลัมน์ / เปลี่ยนจำนวนแถว / เปลี่ยนหน้า ทำงานปกติ
- [ ] ออกจากระบบได้

**มือถือ (กว้าง 360–430px)**
- [ ] ปุ่มแฮมเบอร์เกอร์เปิด sidebar ได้
- [ ] แถบชิปเลื่อนแนวนอนได้ในบรรทัดเดียว ไม่ดันตารางลงลึกเกินไป
- [ ] กดชิปบนมือถือได้ถนัด

---

## 10. Rollback

นำ `Index.html` ที่สำรองไว้ในข้อ 8.1 กลับเข้าไปแทน แล้ว deploy version ใหม่
ไม่มีข้อมูล ไม่มี schema และไม่มีค่าตั้งค่าใดต้องย้อนกลับ เพราะงานนี้ไม่แตะฝั่งเซิร์ฟเวอร์เลย

---

## 11. ขั้นถัดไป

Release B (วิดเจ็ตผู้ช่วยตอบคำถาม) พร้อมเริ่มได้เมื่อ Release A ผ่าน UAT
เมื่อมุมล่างขวาว่างแล้ว วิดเจ็ตจะวางที่ `bottom: 24px; z-index: 1030` ได้ตั้งแต่แรกโดยไม่ต้องทำงานซ้ำ

ก่อนเริ่ม Release B ควรรัน `auditAssistantFeasibility()` เพื่อวัดคุณภาพข้อมูลคอลัมน์ J ของ `ReportNo` ซึ่งเป็นตัวชี้ขาดว่าฟีเจอร์ "งานค้างของฉัน" จะใช้งานได้จริงหรือไม่
