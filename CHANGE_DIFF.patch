--- Index.html.pre-admin-menu-change
+++ Index.html.admin-only-layout-restored
@@ -502,11 +502,11 @@
         <button class="nav-btn" id="adminTemplateMenu" style="display:none" onclick="goPage('PrecheckAdmin')"><i class="fas fa-list-check text-secondary"></i> จัดการแบบตรวจ</button>
       </div>
 
-      <button class="nav-btn legacy-report-flow-btn" onclick="showReportForm()">
+      <button class="nav-btn legacy-report-flow-btn" id="adminLegacyReportMenu" style="display:none" onclick="showReportForm()">
         <i class="fas fa-file-signature text-primary"></i> ส่งรายงานฉบับสมบูรณ์ (Workflow เดิม)
       </button>
       
-      <button class="nav-btn" onclick="showNonCompletedProjectForm()">
+      <button class="nav-btn" id="adminLegacyNonCompletedMenu" style="display:none" onclick="showNonCompletedProjectForm()">
         <i class="fas fa-exclamation-triangle text-danger"></i> แจ้งกิจกรรมที่ไม่ได้ดำเนินการ
       </button>
     </div>
@@ -516,7 +516,7 @@
             <i class="fas fa-sign-out-alt me-2"></i> ออกจากระบบ
         </button>
         <div class="text-center mt-2 text-muted" style="font-size:0.7rem;">
-            v.2.0.0<br>พัฒนาโดยงานแผนงานและสารสนเทศโรงเรียน
+            v.2.1.0<br>พัฒนาโดยงานแผนงานและสารสนเทศโรงเรียน
         </div>
     </div>
   </div>
@@ -925,10 +925,24 @@
         pcClientConfig = ctx || pcClientConfig;
         if (ctx && ctx.chunkSizeBytes) CHUNK_SIZE = Number(ctx.chunkSizeBytes);
         const roles = (ctx && ctx.roles) || [];
-        if (roles.includes('PRECHECK_OFFICER') || roles.includes('PRECHECK_ADMIN')) document.getElementById('officerMenu').style.display = 'block';
-        if (roles.includes('PRECHECK_ADMIN')) document.getElementById('adminTemplateMenu').style.display = 'flex';
-        if (ctx && ctx.enforceReportActivity) document.querySelectorAll('.legacy-report-flow-btn').forEach(el => el.style.display = 'none');
-        if (ctx && ctx.displayName) { document.getElementById('displayUserName').textContent = ctx.displayName; document.getElementById('userAvatar').textContent = ctx.displayName.charAt(0).toUpperCase(); }
+        const isPrecheckAdmin = roles.includes('PRECHECK_ADMIN');
+
+        if (roles.includes('PRECHECK_OFFICER') || isPrecheckAdmin) {
+          document.getElementById('officerMenu').style.display = 'block';
+        }
+        if (isPrecheckAdmin) {
+          document.getElementById('adminTemplateMenu').style.display = 'flex';
+        }
+
+        const adminLegacyReportMenu = document.getElementById('adminLegacyReportMenu');
+        const adminLegacyNonCompletedMenu = document.getElementById('adminLegacyNonCompletedMenu');
+        if (adminLegacyReportMenu) adminLegacyReportMenu.style.display = isPrecheckAdmin ? 'flex' : 'none';
+        if (adminLegacyNonCompletedMenu) adminLegacyNonCompletedMenu.style.display = isPrecheckAdmin ? 'flex' : 'none';
+
+        if (ctx && ctx.displayName) {
+          document.getElementById('displayUserName').textContent = ctx.displayName;
+          document.getElementById('userAvatar').textContent = ctx.displayName.charAt(0).toUpperCase();
+        }
       }).getNavigationContext();
     }
 
