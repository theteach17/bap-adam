# การตีความผลตรวจข้อมูล และการแก้ไขที่ตามมา

**ที่มา:** ผลรัน `auditAssistantFeasibility()` เมื่อ 9 ก.ย. 2569 เวลา 13:00
**ผลกระทบ:** แก้โค้ด Release B เพิ่ม 3 เรื่อง และเพิ่มคอนฟิก 1 คีย์

---

## 1. ผลตรวจ: ผ่านเกณฑ์

| ตัวชี้วัด | ค่า | เกณฑ์ | ผล |
|---|---|---|---|
| เอกสารค้างที่จับคู่เจ้าของได้ | **89.7%** (429/478) | ≥ 85% | ผ่าน |
| คอลัมน์ J มีค่าและรูปแบบถูกต้อง | 99.9% (1071/1072) | — | ดีมาก |
| ขนาดทะเบียน | 1,072 แถว อ่าน 464 ms | < 5,000 | เล็ก แคชต่อผู้ใช้เพียงพอ |
| บัญชีผู้ใช้ | 150 บัญชี มีอีเมลครบทุกบัญชี | — | ดีมาก |

**สรุป: เดินหน้า Phase 1 ได้ทันที** ไม่ต้องแก้ข้อมูลก่อน

---

## 2. สิ่งที่ต้องแก้ในโค้ด — พบจากข้อมูลชุดนี้

### 2.1 เอกสาร 50.2% มี `documentType = UNKNOWN` → ผู้ช่วยเดิมพาผู้ใช้ไปทางตัน

**สิ่งที่พบ**

| ประเภท | จำนวน | สัดส่วน |
|---|---|---|
| `UNKNOWN` | **538** | **50.2%** |
| `REPORT_ACTIVITY` | 477 | 44.5% |
| `OTHER_DOCUMENT` | 31 | 2.9% |
| `NON_COMPLETED_MEMO` | 26 | 2.4% |

`resolveDocumentType_()` คืน `UNKNOWN` เมื่อชื่อเอกสารไม่ขึ้นต้นด้วย `รายงานผลการดำเนินกิจกรรม` / `บันทึกข้อความ` / `เอกสาร` และ **`lookupDocument()` ปฏิเสธเอกสารกลุ่มนี้ด้วย `allowedAction: 'NONE'`**

**ข้อบกพร่องเดิมของผม** — โค้ดเดิมจัดเอกสารกลุ่มนี้เป็น `NOT_STARTED` แล้วขึ้นปุ่ม "ไปหน้าส่งเอกสาร" ผู้ใช้กดแล้วจะโดนระบบปฏิเสธทันที กลายเป็นทางตันที่ผู้ช่วยพาไปเอง ซึ่งแย่กว่าไม่ตอบเลย

**การแก้**

- เพิ่มสถานะ `NAME_NOT_SUPPORTED` พร้อมคำอธิบายไทยและวิธีแก้ที่ถูกต้อง
- เอกสารกลุ่มนี้ **ไม่มีปุ่มพาไปหน้าส่งเอกสาร** แต่เสนอปุ่ม "ติดต่อเจ้าหน้าที่" แทน
- ในรายการงานค้างแยกเป็นกลุ่ม **"ต้องแก้ชื่อในทะเบียนก่อน"** พร้อมคำอธิบายว่าทำไมส่งไม่ได้
- เอกสารชื่อไม่รองรับที่มีไฟล์ฉบับสมบูรณ์แล้ว ยังรายงานว่า "เสร็จสมบูรณ์" ตามปกติ

### 2.2 ทะเบียนมีเอกสารย้อนหลัง 3 ปี → เรียงลำดับผิดทาง

**สิ่งที่พบ**

| ปี | จำนวน | สัดส่วน |
|---|---|---|
| 2567 | 409 | 38.2% |
| 2568 | 551 | 51.4% |
| 2569 | 112 | 10.4% |

โค้ดเดิมเรียงตามเลขเอกสารจากน้อยไปมาก ทำให้เอกสารปี 2567 ขึ้นก่อนงานปีปัจจุบัน ผู้ใช้จะเห็นงานเก่าค้างจาก 2 ปีก่อนขึ้นมาบังงานที่ต้องทำจริง

**การแก้** เรียงปีใหม่สุดขึ้นก่อนเสมอ แล้วเรียงเลขเอกสารจากมากไปน้อยภายในปีเดียวกัน

### 2.3 เพิ่มคอนฟิก `PC_ASSIST_WORKLOAD_FROM_YEAR`

ให้ตั้งได้ว่าจะแสดงงานค้างตั้งแต่ปีใดเป็นต้นไป

- ค่าตั้งต้น `0` = แสดงทุกปี (ไม่เปลี่ยนพฤติกรรมโดยไม่ได้ตั้งใจ)
- ตั้ง `2569` เพื่อโฟกัสงานปีปัจจุบัน ระบบจะแจ้งผู้ใช้ว่าข้ามงานค้างเก่าไปกี่ฉบับ
- ตัวกรองมีผลเฉพาะรายการงานค้าง ส่วนสถิติรวมใน `MY_SUMMARY` ยังนับครบทุกปีเหมือนเดิม

---

## 3. สิ่งที่ควรทำกับข้อมูล (ไม่ใช่เรื่องโค้ด)

### 3.1 อีเมล 140 ฉบับไม่ตรงกับบัญชีผู้ใช้ใด

| รายการ | จำนวน |
|---|---|
| เอกสารที่อีเมลถูกต้องแต่ไม่ตรงบัญชีใด | 140 (13.1%) |
| ในจำนวนนั้นเป็นเอกสารที่ยังค้าง | **48** (10.0% ของงานค้าง) |
| 15 อีเมลแรกครอบคลุม | 84 จาก 140 ฉบับ (60%) |

**ข้อสังเกตสำคัญ** รายชื่ออีเมลที่ไม่ตรงมี **2 รูปแบบปนกัน**

- แบบชื่อ: `phanuwat_koe@`, `sirirat.kae@`, `kanokon.p@`, `kru.papleng@`
- แบบรหัสตัวเลข: `7011@`, `85838@`, `3135@`, `85842@`, `85912@`

รูปแบบที่ปนกันแบบนี้มักเกิดจาก **การเปลี่ยนรูปแบบอีเมลของโรงเรียน** ไม่ใช่คนที่ไม่มีบัญชีจริง

**ข้อเสนอ** ตรวจสอบ 15 อีเมลแรกว่าเจ้าของมีบัญชีในชีต `Credential` ด้วยอีเมลรูปแบบอื่นหรือไม่ ถ้าใช่ การแก้ข้อมูลทะเบียนเพียง 15 รายการจะกู้คืนได้ถึง 60% ของส่วนที่หายไป ซึ่งคุ้มค่ามากเทียบกับการแก้ทีละฉบับ

ระหว่างที่ยังไม่แก้ ผู้ช่วยได้ป้องกันไว้แล้ว 2 ชั้น คือจับคู่ด้วยชื่อผู้รับผิดชอบเป็นตัวสำรอง และแสดงข้อความกำกับท้ายทุกรายการว่าอาจไม่ครบถ้วน

### 3.2 เอกสาร 1 ฉบับไม่ระบุอีเมลเจ้าของ

`บง 033/2567 | นายคเณศ นามนัย` — เป็นของท่านเอง และการจับคู่ด้วยชื่อ (`PC_ASSIST_NAME_MATCH=true`) ครอบคลุมกรณีนี้อยู่แล้ว

### 3.3 ยังไม่ทราบขนาดกลุ่ม "ต้องแก้ชื่อในทะเบียนก่อน" ที่แท้จริง

รายงานเดิมบอกว่ามี `UNKNOWN` 538 ฉบับทั้งทะเบียน แต่ไม่ได้บอกว่ากี่ฉบับในนั้นยังค้างอยู่ ซึ่งคือตัวเลขที่กำหนดว่ากลุ่มใหม่นี้จะใหญ่แค่ไหนสำหรับผู้ใช้จริง

ผมจึงขยาย `AssistantFeasibilityAudit.gs` ให้รายงานเพิ่ม

- ประเภทเอกสารแยก **ทั้งหมด / เฉพาะที่ยังไม่เสร็จ**
- ปีเอกสารแยก **ทั้งหมด / เฉพาะที่ยังไม่เสร็จ** (ใช้ตัดสินใจตั้ง `PC_ASSIST_WORKLOAD_FROM_YEAR`)
- ตัวอย่างเอกสารที่ติดปัญหาชื่อ 10 รายการ
- ข้อเสนอค่าคอนฟิกที่เหมาะกับข้อมูลชุดนั้น

**กรุณารันสคริปต์ฉบับใหม่นี้อีกครั้ง** ยังเป็น read-only ล้วนเหมือนเดิม

---

## 4. ผลการตรวจสอบหลังแก้ (รอบที่ 8–9)

| ชุดตรวจ | เคส | ผล |
|---|---|---|
| ไวยากรณ์ `.gs` 10 ไฟล์ | — | ผ่าน |
| โครงสร้าง `Index.html` + JS | — | ผ่าน |
| `AssistantWidget.html` (CSS + JS) | — | ผ่าน |
| พฤติกรรมฝั่งเซิร์ฟเวอร์ | **184** | ผ่านทั้งหมด |
| regression ระบบเดิม | 33 | ผ่านทั้งหมด |
| end-to-end วิดเจ็ต | 58 | ผ่านทั้งหมด |
| encoding ภาษาไทย 12 ไฟล์ | — | ผ่าน |

**รวม 275 เคส ผ่านทั้งหมด**

เคสที่เพิ่มใหม่ 23 เคสในรอบนี้ครอบคลุม: เอกสารชื่อไม่รองรับ (14 เคส) · การเรียงลำดับปี (2) · ตัวกรองปี (5) · ความครบถ้วนของพจนานุกรมสถานะ (2)

### ข้อบกพร่องที่พบเพิ่มและแก้แล้วในรอบนี้

| สิ่งที่พบ | ความรุนแรง | การแก้ |
|---|---|---|
| เอกสารชื่อไม่รองรับถูกตอบว่า "ยังไม่เริ่มดำเนินการ" แล้วพาไปหน้าที่ระบบปฏิเสธ | **สูง** | เพิ่มสถานะ `NAME_NOT_SUPPORTED` ตัดปุ่มที่ใช้ไม่ได้ออก เสนอช่องทางติดต่อแทน |
| เรียงเอกสารเก่าขึ้นก่อนงานปีปัจจุบัน | กลาง | เรียงปีใหม่สุดขึ้นก่อน |
| ตัวเลข "ข้ามเอกสารเก่า N ฉบับ" นับรวมเอกสารที่เสร็จแล้ว ทำให้เข้าใจผิด | กลาง | ย้ายการกรองปีไปหลังคำนวณสถานะ ให้ N หมายถึงงานค้างจริง |
| สถานะ `NOT_FOUND` ตกไปอยู่กลุ่ม `OTHER` โดยไม่ตั้งใจ | ต่ำ | จัดกลุ่มให้ชัดเจน |

---

## 5. ค่าคอนฟิกที่แนะนำสำหรับ Phase 1

```
PC_ASSIST_ENABLED            = true
PC_ASSIST_PILOT_EMAILS       = <อีเมลทีมงาน 3–5 คน>
PC_ASSIST_STATUS_SCOPE       = ORG
PC_ASSIST_NAME_MATCH         = true
PC_ASSIST_WORKLOAD_FROM_YEAR = 0          ← ดูผล audit รอบใหม่ก่อนค่อยตัดสินใจ
PC_ASSIST_RATE_PER_MIN       = 20
PC_ASSIST_LOG_RETENTION_DAYS = 90
PC_ASSIST_CONTACT_TEXT       = <ช่องทางติดต่องานแผนงานและสารสนเทศ>
```

`PC_ASSIST_CONTACT_TEXT` สำคัญขึ้นกว่าเดิมมากในรอบนี้ เพราะกลุ่ม "ต้องแก้ชื่อในทะเบียนก่อน" จะถูกส่งต่อไปหาเจ้าหน้าที่ ถ้าเว้นว่างไว้ผู้ใช้จะไม่รู้ว่าต้องติดต่อใคร

---

## 6. สิ่งที่ต้องทำต่อ

1. รัน `AssistantFeasibilityAudit.gs` ฉบับใหม่ เพื่อดูตัวเลข `UNKNOWN` ที่ยังค้าง และปีที่ควรใช้เป็นจุดตัด
2. ตรวจ 15 อีเมลแรกในหัวข้อ [8] ว่าเจ้าของมีบัญชีด้วยอีเมลรูปแบบอื่นหรือไม่
3. ตั้ง `PC_ASSIST_CONTACT_TEXT` ให้เรียบร้อยก่อนเปิด Pilot
4. ติดตั้งไฟล์ทั้ง 12 โดยคง `PC_ASSIST_ENABLED = false` แล้วยืนยันว่าหน้าหลักไม่มีอะไรเปลี่ยน
5. รัน `verifyAssistantInstallation()` · `verifyPrecheckInstallation()` · `runPrecheckHealthCheck()`
6. เปิด Phase 1
