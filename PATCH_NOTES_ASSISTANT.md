# PATCH NOTES — ASSISTANT MODULE (Release B)

**ระบบ:** Project Adam — ศูนย์สารสนเทศกลาง
**โมดูล:** ผู้ช่วยตอบคำถามอัตโนมัติ v1.0.0
**ต่อยอดจาก:** Release A (STATBAR) ที่ผ่าน UAT แล้ว

---

## 1. สรุปสิ่งที่ได้

วิดเจ็ตแชทลอยมุมล่างขวาของหน้าหลัก ผู้ใช้กดเปิด/พับได้ ถามแล้วระบบตอบจากข้อมูลจริงในฐานข้อมูลทันที

**ตอบได้ 18 เจตนา แบ่งเป็น 5 กลุ่ม**

| กลุ่ม | เจตนา | ตัวอย่างคำถาม |
|---|---|---|
| ขั้นตอนการใช้งาน | `HOWTO_START`, `HOWTO_REGISTER`, `HOWTO_SUBMIT_REPORT`, `HOWTO_NON_COMPLETED`, `HOWTO_REVISION`, `HOWTO_FILE_RULES`, `HOWTO_CONTACT` | "เริ่มต้นใช้งานระบบต้องทำอะไรก่อน" |
| เอกสารรายฉบับ | `DOC_STATUS`, `DOC_NEXT_ACTION`, `DOC_FIX_LIST`, `DOC_FINAL_LINK`, `DOC_HISTORY` | "บง 123/2569 อยู่ขั้นตอนไหน" |
| งานของฉัน | `MY_PENDING`, `MY_NEEDS_FIX`, `MY_IN_REVIEW`, `MY_SUMMARY` | "เอกสารค้างส่งของฉันมีอะไรบ้าง" |
| เจ้าหน้าที่ | `QUEUE_SUMMARY` | "ภาพรวมคิวตรวจ" |
| สำรอง | `NEED_DOCUMENT_NUMBER`, `UNKNOWN` | ถามกลับ / เสนอทางเลือก |

**สถาปัตยกรรม** ใช้ Intent Engine แบบกำหนดผลลัพธ์ได้ ไม่เรียก LLM ไม่เชื่อมต่อระบบภายนอกใด ๆ ไม่เพิ่ม OAuth scope

---

## 2. ไฟล์ที่ต้องนำขึ้นระบบ (11 ไฟล์)

### สร้างใหม่ 7 ไฟล์

| ไฟล์ | บรรทัด | หน้าที่ |
|---|---|---|
| `AssistantConstants.gs` | 187 | ค่าคงที่ + พจนานุกรมสถานะไทย 15 สถานะ |
| `AssistantKnowledge.gs` | 134 | เนื้อหาขั้นตอนการใช้งานภาษาไทย (แก้ถ้อยคำได้โดยไม่แตะ logic) |
| `AssistantNlu.gs` | 160 | ตัวจำแนกเจตนาภาษาไทย + สกัดเลขเอกสาร |
| `AssistantDataService.gs` | 273 | ชั้นอ่านข้อมูล **READ-ONLY** + แคชต่อผู้ใช้ |
| `AssistantService.gs` | 618 | public endpoint 4 ตัว + จุดตรวจสิทธิ์ + ประกอบคำตอบ |
| `AssistantSetup.gs` | 170 | ติดตั้ง/ตรวจสอบ/ล้าง log/รายงานการใช้งาน |
| `AssistantWidget.html` | 486 | วิดเจ็ตแชท เบ็ดเสร็จในตัวเอง |

### แทนที่ไฟล์เดิม 4 ไฟล์ (แก้รวม 29 บรรทัด ทั้งหมดเป็นการเพิ่ม)

| ไฟล์ | ต่าง | รายละเอียด |
|---|---|---|
| `Index.html` | 5 บรรทัด | เพิ่ม `<?!= include_('AssistantWidget'); ?>` ก่อน `</body>` และเรียก `pcwSetAvailable()` ใน `loadNavigationContext()` |
| `Router.gs` | 7 บรรทัด | เพิ่ม property `assistantEnabled` ใน `getNavigationContext()` (ห่อ try/catch ไว้แล้ว) |
| `PrecheckConstants.gs` | 7 บรรทัด | เพิ่ม key `PC_AssistantLog` **ท้ายสุด** ของ `PC_HEADERS` |
| `ConfigService.gs` | 10 บรรทัด | เพิ่ม 7 คีย์ `PC_ASSIST_*` ใน whitelist ทั้ง 2 ชุด |

> `Index.html` ในชุดนี้รวม Release A ไว้แล้ว แทนที่ทับได้เลย

### ไม่ต้องแตะเลย

`appsscript.json` · OAuth scopes · `รหัส.gs` · `PrecheckService.gs` · `PrecheckReviewService.gs` · `PrecheckCommitService.gs` · `PrecheckVersionService.gs` · `UploadService.gs` · `DriveService.gs` · `MigrationService.gs` · `HealthCheckService.gs` · `BackupService.gs` · `RecoveryService.gs` · `AuditService.gs` · `AccessControlService.gs` · `PrecheckRepository.gs` · `DocumentRepository.gs` · `Utils.gs` · `PerfCache.gs` · หน้า HTML อื่นทุกหน้า · schema ชีตเดิม · trigger เดิม

---

## 3. หลักประกันว่าไม่กระทบระบบเดิม

### 3.1 READ-ONLY พิสูจน์ด้วยการรันจริง

ชุดทดสอบเรียกทุกเจตนาครบทุกตัว แล้วนับจำนวนคำสั่งเขียนที่เกิดขึ้นจริงกับทุกชีต

| ชีต | จำนวนการเขียน |
|---|---|
| `ReportNo`, `ReportSubmit`, `Credential`, `Settings Sheet` | **0** |
| `PC_Submissions`, `PC_Versions`, `PC_Reviews`, `PC_ReviewResponses` | **0** |
| `PC_Templates`, `PC_TemplateItems`, `PC_Access`, `PC_Notifications` | **0** |
| `PC_AssistantLog` | เขียน (ชีตของโมดูลนี้เอง) |
| `PC_Audit` | เขียนเฉพาะคำถามที่เข้าถึงข้อมูลเอกสาร ตามกลไกตรวจสอบเดิมของระบบ |

### 3.2 พฤติกรรมเดิมไม่เปลี่ยน

เทียบผลลัพธ์ระหว่างไฟล์ต้นฉบับกับไฟล์ที่แก้แล้ว ด้วยข้อมูลชุดเดียวกัน

- `getNavigationContext()` — field เดิมครบทุกตัว ค่าไม่เปลี่ยน เพิ่มเฉพาะ `assistantEnabled`
- `lookupDocument()` — ผลลัพธ์ตรงกันทุกตัวอักษร ทดสอบ 11 เลขเอกสารครอบคลุมทุกประเภทและทุกสถานะ
- `getMyDocuments()`, `getPrecheckClientConfig()` — ผลลัพธ์เหมือนเดิม
- `getPrecheckConfig_()` — ไม่มีคีย์หาย ค่าไม่เปลี่ยน และ **ไม่มีคีย์ผู้ช่วยปนเข้ามา**
- `PC_HEADERS` — ชีตเดิม 10 ตัวคงลำดับเดิม คอลัมน์ไม่เปลี่ยนแม้แต่ตัวเดียว
- `configurePrecheckSystem_()` — คีย์เดิม 25 ตัวครบ และยังปฏิเสธคีย์นอก whitelist เหมือนเดิม

### 3.3 ติดตั้งชีตใหม่โดยอัตโนมัติ

`pcInitializeWorkflowSheets_` วนลูป `Object.keys(PC_HEADERS)` อยู่แล้ว การเพิ่ม key ท้ายสุดจึงได้ผลนี้ฟรี

- ฐานติดตั้งใหม่ (มีแค่ `Sheet1`) — สร้างครบ ชีตแรกยังเป็น `PC_Submissions` เหมือนเดิม
- ฐานเดิมที่ใช้งานอยู่ — เพิ่มเฉพาะ `PC_AssistantLog` ไม่เขียนทับ header ชีตเดิมแม้แต่ชีตเดียว
- รันซ้ำได้ (idempotent)
- `runPrecheckHealthCheck()` ตรวจ schema ชีตใหม่ให้เองโดยไม่ต้องแก้โค้ด

### 3.4 fail-closed ทุกทาง

- วิดเจ็ตซ่อนไว้เป็นค่าตั้งต้น จะแสดงก็ต่อเมื่อเซิร์ฟเวอร์ยืนยันว่าเปิดใช้งานสำหรับบัญชีนั้น
- โมดูลผู้ช่วยพังหรือยังไม่ได้ติดตั้ง → `getNavigationContext()` ยังทำงานปกติ คืน `assistantEnabled=false`
- ยังไม่ได้สร้างชีต log → ผู้ช่วยยังตอบคำถามได้ บันทึกข้อผิดพลาดลง console แทนการล้ม
- ยังไม่ได้ตั้ง `PC_DB_ID` → ยังตอบสถานะจากทะเบียนเอกสารได้

---

## 4. ความปลอดภัย

### 4.1 ลำดับการตรวจ (ทุก request ผ่านครบ)

1. ตัวตนผู้ใช้ผ่าน `getCurrentPrincipal_()` เดิมของระบบ
2. สวิตช์ `PC_ASSIST_ENABLED` + ขอบเขต `PC_ASSIST_PILOT_EMAILS`
3. จำกัดอัตรา (ค่าตั้งต้น 20 คำถาม/นาที/คน) — การให้คะแนนไม่กินโควตา
4. ความยาวข้อความสูงสุด 500 ตัวอักษร
5. สิทธิ์ระดับเจตนา (intent ของเจ้าหน้าที่ถูกซ่อนและถูกปฏิเสธเมื่อเรียกตรง)
6. สิทธิ์ระดับข้อมูล (`pcCanViewSubmission_` เดิม)

### 4.2 เส้นแบ่งข้อมูล

| ข้อมูล | ผู้ใช้ทั่วไปเห็นของผู้อื่น |
|---|---|
| ชื่อเอกสาร กลุ่มบริหาร สถานะ | **ได้** (ตรงกับ `lookupDocument()` และ `getData()` ที่เปิดอยู่เดิม) |
| อีเมลเจ้าของเอกสาร | **ไม่ได้** — ปิดบังเสมอแม้ `lookupDocument()` เดิมจะคืนมา |
| ผลการตรวจ / รายการที่ต้องแก้ / ความเห็นเจ้าหน้าที่ | **ไม่ได้** — ใช้ `pcCanViewSubmission_` เดิม |
| ประวัติการส่ง | **ไม่ได้** |
| รายการงานค้างของผู้อื่น | **ไม่ได้** |
| KPI คิวตรวจ | เฉพาะ OFFICER/ADMIN |

ตั้ง `PC_ASSIST_STATUS_SCOPE = OWN` เพื่อเข้มขึ้นได้ทุกเมื่อโดยไม่ต้องแก้โค้ด

### 4.3 การป้องกันเชิงเทคนิค

- เซิร์ฟเวอร์คืน **JSON เท่านั้น ไม่คืน HTML** — พิสูจน์ด้วยเทสต์ที่สแกนทุกบล็อกในทุกคำตอบ
- ฝั่งหน้าเว็บเรนเดอร์ด้วย `textContent` และ DOM API เท่านั้น ไม่ใช้ `innerHTML` กับข้อความผู้ใช้
- ปุ่มนำทางชี้ได้เฉพาะหน้าที่อยู่ใน allowlist เดิม
- ลิงก์ภายนอกต้องเป็น `https://` เท่านั้น พร้อม `rel="noopener noreferrer"`
- ข้อความ "ไม่พบเอกสาร" กับ "ไม่มีสิทธิ์" ใช้ถ้อยคำเดียวกัน ป้องกันการสุ่มเลขเอกสารหาข้อมูล
- `intent` ตรวจด้วย `hasOwnProperty` ป้องกันการอ้าง property ของ prototype

---

## 5. ประสิทธิภาพ

| มาตรการ | ผล |
|---|---|
| โหลดค่าตั้งต้นแบบ lazy | **หน้าหลักไม่มีการเรียกเซิร์ฟเวอร์เพิ่มแม้แต่ครั้งเดียว** จนกว่าผู้ใช้จะกดเปิดวิดเจ็ต |
| แคชรายการงานต่อผู้ใช้ 180 วินาที | สแกน `ReportNo` ครั้งเดียวต่อ 3 นาที มีปุ่มรีเฟรชข้ามแคชได้ |
| แคช KPI คิวตรวจ 90 วินาที | ตรงกับ dashboard เดิม |
| อ่านชีตแบบ batch + memo ต่อ execution | ใช้กลไก `pcListObjects_` / `pcMemo_` เดิม ไม่สร้างของใหม่ |
| จำกัดขนาดแคช | ทดสอบด้วยข้อมูล 400 แถว ได้ 20 KB (เพดาน 100 KB) |
| บันทึก `PC_Audit` เฉพาะคำถามที่เข้าถึงข้อมูลเอกสาร | ป้องกัน `PC_Audit` บวมจนกระทบ `pcFilterObjects_(PC_Audit)` ที่ระบบแจ้งเตือนเดิมใช้อยู่ |
| ล้าง log แบบ batch | เขียน 1 ครั้ง + ลบ 1 ครั้ง ต่อการล้าง ไม่ใช่ลบทีละแถว |

---

## 6. ผลการตรวจสอบ (7 รอบ, 252 เคส)

| ชุดตรวจ | เคส | ผล |
|---|---|---|
| ไวยากรณ์ `.gs` 9 ไฟล์ | — | ผ่าน |
| โครงสร้าง `Index.html` + JS | — | ผ่าน (tag สมดุล, CSS 79/79, 2 script blocks) |
| `AssistantWidget.html` (CSS parser + JS) | — | ผ่าน (63 rules, 0 error) |
| พฤติกรรมฝั่งเซิร์ฟเวอร์ | 161 | ผ่านทั้งหมด |
| regression ระบบเดิม | 33 | ผ่านทั้งหมด |
| end-to-end วิดเจ็ต + เซิร์ฟเวอร์จริงใน DOM | 58 | ผ่านทั้งหมด |
| encoding ภาษาไทย 11 ไฟล์ | — | ผ่าน (ไม่มีวรรณยุกต์ลอย/อักขระแปลกปลอม) |

**อัตราจับเจตนา: 62/62 ประโยคภาษาไทย = 100%** (เกณฑ์ที่ตั้งไว้ 85%)

### ข้อบกพร่องที่พบระหว่างตรวจและแก้แล้ว

| รอบ | สิ่งที่พบ | ความรุนแรง | การแก้ |
|---|---|---|---|
| 1 | คำถามสั้นแต่ชัดเจน เช่น "สถานะเอกสาร" ตกเกณฑ์คะแนนกลายเป็นตอบไม่ได้ | กลาง | รับเจตนาที่ไม่มีคู่แข่งแม้คะแนนต่ำ |
| 1 | คำว่า "ยังไง" กว้างเกินไป ทำให้คำถามทั่วไปถูกจัดเป็นถามสถานะ | กลาง | ถอดคำสำคัญนั้นออก |
| 1 | `verifyAssistantInstallation()` มองหาข้อความผิดที่ | ต่ำ | ตรวจ `include_` ใน Index และตรวจไฟล์วิดเจ็ตแยก |
| 2 | "ไฟล์ฉบับสมบูรณ์...อยู่ไหน" ได้คะแนนเสมอกันกับถามสถานะ | กลาง | เปลี่ยนน้ำหนักคำสำคัญให้แปรตามความยาว วลีที่เฉพาะเจาะจงกว่าชนะ |
| 2 | เอ่ยเลขเอกสารแต่ถูกตอบเป็นคำถามเชิงวิธีใช้ | กลาง | ลดน้ำหนัก `HOWTO_*` เมื่อมีเลขเอกสารในคำถาม |
| 5 | จำกัดอัตราใช้ TTL ที่รีเซ็ตทุกครั้งที่เขียน ทำให้คนถามห่าง ๆ ถูกล็อก | **สูง** | เก็บเวลาเริ่มหน้าต่างไว้ด้วย |
| 5 | การให้คะแนนคำตอบกินโควตาการถาม | กลาง | แยกเส้นทาง ไม่นับเป็นคำถาม |
| 5 | `AS_CONST.INTENT[wanted]` เข้าถึง property ของ prototype ได้ | กลาง | ใช้ `hasOwnProperty` |
| 5 | เขียน `PC_Audit` ทุกคำถาม จะบวมจนกระทบระบบแจ้งเตือนเดิม | **สูง** | บันทึกเฉพาะคำถามที่เข้าถึงข้อมูลเอกสาร |
| 5 | `purgeAssistantLogs` ลบทีละแถว เสี่ยงชนเพดาน 6 นาที | กลาง | เขียนทับ + ลบท้ายครั้งเดียว (ทดสอบ 500 แถว ใช้ 2 การเขียน) |
| 5 | แคชอาจเกินเพดาน 100 KB เมื่อข้อมูลเยอะ | กลาง | จำกัด 30 รายการ/กลุ่ม + ตัดชื่อเอกสารที่ 120 ตัวอักษร |
| 6 | **รายงานงานค้างเป็น 32 ฉบับ ทั้งที่มีจริง 399 ฉบับ** | **สูง** | แยกจำนวนจริงออกจากจำนวนที่แสดง รายงานจำนวนจริงเสมอ |

---

## 7. คอนฟิกใหม่ 7 คีย์

ตั้งค่าได้จาก Settings Sheet แล้วรัน `configurePrecheckSystem()` หรือตั้งตรงใน Script Properties

| คีย์ | ค่าตั้งต้น | ความหมาย |
|---|---|---|
| `PC_ASSIST_ENABLED` | `false` | สวิตช์เปิด/ปิดโมดูลทั้งหมด |
| `PC_ASSIST_PILOT_EMAILS` | ว่าง | ถ้ามีค่า จะเปิดเฉพาะอีเมลในรายการ (ADMIN ใช้ได้เสมอ) |
| `PC_ASSIST_STATUS_SCOPE` | `ORG` | `ORG` = ถามสถานะเอกสารใครก็ได้ (เท่าพฤติกรรมเดิม) · `OWN` = เฉพาะของตัวเอง |
| `PC_ASSIST_NAME_MATCH` | `true` | จับคู่ผู้รับผิดชอบจากชื่อเมื่ออีเมลไม่ตรง (มีป้ายกำกับให้ผู้ใช้เห็นเสมอ) |
| `PC_ASSIST_CONTACT_TEXT` | ว่าง | ข้อความช่องทางติดต่อเจ้าหน้าที่ที่จะแสดงในคำตอบ |
| `PC_ASSIST_RATE_PER_MIN` | `20` | จำนวนคำถามสูงสุดต่อนาทีต่อคน |
| `PC_ASSIST_LOG_RETENTION_DAYS` | `90` | อายุการเก็บบันทึกคำถาม |

---

## 8. ขั้นตอนติดตั้ง

1. **สำรอง** source code ปัจจุบันเป็น ZIP และสำเนา Workflow DB
2. วางไฟล์ใหม่ 7 ไฟล์
3. แทนที่ไฟล์เดิม 4 ไฟล์ (`Index.html`, `Router.gs`, `PrecheckConstants.gs`, `ConfigService.gs`)
4. ยืนยันว่า `PC_ASSIST_ENABLED` ยังเป็น `false` (ค่าตั้งต้น)
5. รัน `setupPrecheckSystem()` → สร้างชีต `PC_AssistantLog` อัตโนมัติ
6. รัน `verifyAssistantInstallation()` → ต้องผ่านทุกข้อ
7. รัน `verifyPrecheckInstallation()` และ `runPrecheckHealthCheck()` → ต้องไม่มี fail
8. Deploy version ใหม่ แล้วแก้ deployment เดิมให้ชี้ version ใหม่ (รักษา URL เดิม)
9. **ตรวจว่าหน้าหลักยังไม่มีอะไรเปลี่ยนเลย** ← จุดพิสูจน์ว่าไม่กระทบระบบเดิม
10. ตั้งค่า `PC_ASSIST_CONTACT_TEXT` แล้วเปิด Pilot ตามข้อ 9

---

## 9. แผน Rollout ที่แนะนำ

| ระยะ | การตั้งค่า | เกณฑ์ผ่าน |
|---|---|---|
| **Phase 0** สัปดาห์ 1 | `PC_ASSIST_ENABLED=false` | ผู้ใช้ไม่เห็นอะไรเปลี่ยนเลย · workflow เดิมครบ 3 ประเภทเอกสาร |
| **Phase 1** สัปดาห์ 2 | `ENABLED=true` + `PILOT_EMAILS=<ทีมงาน 3–5 คน>` | ทดสอบครบทุกเจตนา · ไม่มีคำตอบผิดข้อเท็จจริงแม้แต่ครั้งเดียว |
| **Phase 2** สัปดาห์ 3–4 | เพิ่ม PILOT เป็น 1 กลุ่มบริหาร (~20–30 คน) | อัตราจับเจตนา ≥ 85% · 👍 ≥ 70% · ไม่มีรายงานข้อมูลรั่ว |
| **Phase 3** สัปดาห์ 5 | ล้าง `PILOT_EMAILS` เปิดทั้งโรงเรียน | ติดตาม `reportAssistantUnmatched()` รายสัปดาห์ต่อเนื่อง 1 เดือน |

**Rollback**

| ระดับ | วิธี | เวลา |
|---|---|---|
| ปิดฟีเจอร์ | `PC_ASSIST_ENABLED=false` | < 1 นาที ไม่ต้อง redeploy |
| จำกัดผู้ใช้ | ตั้ง `PILOT_EMAILS` กลับ | < 1 นาที |
| ถอยโค้ด | redeploy version ก่อนหน้า | ~5 นาที |

ห้ามลบชีต `PC_AssistantLog` เพื่อ rollback (ยึดหลักเดียวกับ `MIGRATION.md` เดิม)

---

## 10. รายการตรวจหลังติดตั้ง

**ขณะ `PC_ASSIST_ENABLED=false`**
- [ ] หน้าหลักไม่มีปุ่มลอย ไม่มีอะไรเปลี่ยน
- [ ] แถบชิปสถิติ Release A ยังทำงานปกติ
- [ ] ขอเลขทะเบียน → ส่งเอกสาร → เจ้าหน้าที่ตรวจ → commit ครบ workflow
- [ ] `runPrecheckHealthCheck()` ผ่าน

**หลังเปิด `PC_ASSIST_ENABLED=true`**
- [ ] ปุ่มลอยปรากฏมุมล่างขวา กดเปิด/พับได้
- [ ] ทักทายด้วยชื่อผู้ใช้ถูกคน · มีปุ่มลัด 5 ปุ่ม
- [ ] "บง XXX/2569 อยู่ขั้นตอนไหน" → ตอบสถานะไทย + บอกว่าอยู่ที่ใคร + มีปุ่มพาไปทำต่อ
- [ ] "เอกสารค้างส่งของฉัน" → รายการถูกต้อง ตรงกับความจริง
- [ ] ปุ่มในคำตอบพาไปหน้าที่ถูกต้อง
- [ ] เอกสารของผู้อื่น → ดูสถานะได้ แต่ดูผลการตรวจไม่ได้
- [ ] เปิดฟอร์มขอเลขทะเบียน → วิดเจ็ตหลบไปอยู่หลัง modal (ไม่บัง)
- [ ] เปิดเมนูมือถือ → วิดเจ็ตหลบไปอยู่หลัง overlay
- [ ] มือถือกว้าง 360px → กล่องแชทใช้งานได้ ไม่ล้นจอ

**ดูแลต่อเนื่อง**
- [ ] ตั้ง trigger รายสัปดาห์เรียก `purgeAssistantLogs()`
- [ ] ทบทวน `reportAssistantUnmatched()` ทุกสัปดาห์เพื่อเพิ่มคำสำคัญ

---

## 11. ข้อควรทราบ

1. **แคชรายการงาน 180 วินาที** — หลังส่งเอกสารใหม่ ตัวเลขอาจช้าได้ถึง 3 นาที มีปุ่ม "รีเฟรชข้อมูล" ให้กดข้ามแคช และทุกคำตอบแสดงเวลาข้อมูลกำกับเสมอ
2. **การจับคู่ผู้รับผิดชอบ** — ใช้อีเมลเป็นหลัก เสริมด้วยชื่อแบบตรงเป๊ะหลังตัดคำนำหน้า รายการที่มาจากการจับคู่ชื่อมีป้ายกำกับ และทุกคำตอบมีข้อความเตือนว่ารายการอาจไม่ครบหากทะเบียนไม่ได้ระบุอีเมล → **ควรรัน `auditAssistantFeasibility()` ก่อนเปิด Phase 2** เพื่อวัดคุณภาพคอลัมน์ J
3. **บทสนทนารีเซ็ตเมื่อเปลี่ยนหน้า** — เป็นผลจากข้อกำหนดที่ให้วิดเจ็ตอยู่หน้าหลักหน้าเดียว คำตอบทุกข้อจึงออกแบบให้จบในตัวเอง
4. **ผู้ช่วยแก้ไขหรือส่งเอกสารแทนไม่ได้** — เป็นข้อห้ามเชิงสถาปัตยกรรมถาวร มีข้อความกำกับใต้ช่องพิมพ์
5. **เอกสาร workflow เดิม** (`บันทึกข้อความชี้แจง` / `เอกสารอื่น`) ไม่มีฟิลด์สถานะใน schema จึงตอบได้เพียง "ส่งแล้ว/ยังไม่ส่ง" ซึ่งเป็นข้อจำกัดของข้อมูล ไม่ใช่ของผู้ช่วย

---

## 12. ขยายไปหน้าอื่นในอนาคต

`AssistantWidget.html` เบ็ดเสร็จในตัวเอง (CSS ทุก selector อยู่ใต้ `#pcAssistantWidget` หรือ `.pcw-` ไม่มี selector ระดับ element) และมีตัวช่วยนำทางที่รองรับทั้ง `goPage()` ของ Index และ `nav()` ของหน้า Precheck

การเพิ่มลงหน้าอื่นจึงใช้เพียง 2 บรรทัดต่อหน้า

```html
<?!= include_('AssistantWidget'); ?>
<script>pcwSetAvailable(true);</script>
```
